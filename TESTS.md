# How to get demo data?

Excute the following command in the Web Console:

```
window.__socket.send(JSON.stringify({"type":"AllKBVExamples", "folder": "../src/test/resources/kbv-zip"}))
```

![](img/get-demo-data.png)